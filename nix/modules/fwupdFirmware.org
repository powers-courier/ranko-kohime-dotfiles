#+title: fwupd Firmware Updater
#+author: Ranko Kohime
#+email: ranko.kohime@runbox.com
#+startup: content
#+property: header-args :mkdirp yes :tangle-mode (identity #o444) :comments no :padline no :noweb yes

#+begin_src nix :tangle fwupdFirmware.nix
{ config, lib, ... }: {
  options.fwupdFirmware.enable = lib.mkEnableOption "Enable Firmware update daemon (fwupd)" // { default = true; };
  config = lib.mkIf config.fwupdFirmware.enable {
    services.fwupd = {
      daemonSettings = {
        IgnorePower = true;
      };
      enable = true;
#      extraRemotes = [];
#      uefiCapsuleSettings = {};
    };
  };
}
#+end_src

